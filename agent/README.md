# agent/

Go cross-platform endpoint agent. Scaffolded in Phase 3.

Cross-compile targets: `windows/amd64`, `linux/amd64`, `darwin/arm64` via
`goreleaser`. mTLS gRPC stream to `mcp/agents`.

Placeholder during Phase 0–2.
