# apps/conductor

Server-side AI Conductor. FastAPI + `anthropic` SDK (Opus 4.7) running named workflows
against the MCP proxy at `http://localhost:7100/mcp`.

Scaffolded in Phase 4. The system prompt skeleton lives at
`apps/conductor/prompts/system_v1.md` (created in Phase 4 from the addendum §D text).

Placeholder during Phase 0–3.
