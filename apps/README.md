# apps/

Top-level applications.

- `web/` — Next.js 15 web UI + BFF (Phase 5).
- `conductor/` — Python FastAPI server-side AI Conductor and workflow runner (Phase 4).

Each app is independently buildable but shares schemas from `packages/schemas/`.
